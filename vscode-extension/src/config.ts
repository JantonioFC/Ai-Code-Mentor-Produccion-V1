export const CONFIG = {
    // Default to localhost for development. 
    // In the future, this could be read from vscode.workspace.getConfiguration()
    API_BASE_URL: 'http://localhost:3000',

    // Auth constants
    AUTH_PROVIDER_ID: 'ai-code-mentor',
    TOKEN_KEY: 'ai-code-mentor-auth-token'
};
