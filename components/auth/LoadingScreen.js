/**
 * LoadingScreen - DISABLED (Nuclear Option)
 * 
 * Este componente ha sido deshabilitado intencionalmente para cumplir con el requerimiento:
 * "La animación sigue cargándose, eliminémosla completamente del código. Que no aparezca en ningún momento."
 * 
 * Se mantiene el archivo para evitar errores de importación en otros lugares del código que no hayan sido refactorizados,
 * pero renderiza NULL.
 */

const LoadingScreen = () => {
  return null;
};

export default LoadingScreen;
